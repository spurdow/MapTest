<menu xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- Search, should appear as action button -->
    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_directions" android:icon="@android:drawable/ic_menu_directions" android:title="Directions" android:showAsAction="always"></item><item android:id="@+id/action_edit" android:icon="@android:drawable/ic_menu_edit" android:title="Edit" android:showAsAction="always"></item><item android:id="@+id/action_settings"
          android:title="Settings"
          android:showAsAction="always" />
    
    
</menu>